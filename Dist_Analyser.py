# Gets handed a numpy array which is a single column of the population data.

# Creates a Histogram of the data

# Plots the theoretical log-normal distribution on the histogram
    # Calculates the parameters
    # Adds the plot to histogram plot

# Creates a Pareto
    # Calculate WHERE the Pareto is a better fit
        # KS test
    # Calculates the parameters at this point
    # Adds the plot to histogram plot